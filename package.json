{
  "name": "brotown-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "smoke": "node scripts/smoke-test.mjs",
    "loadtest": "node scripts/load-test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.96.0"
  }
}
